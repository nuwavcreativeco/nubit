node_modules
.next
.env.local
.DS_Store
*.tsbuildinfo

# Local tooling — installed skills and Claude Code preview config, not app source
.claude
.agents
supabase/.claude
.vercel
.env*

# Supabase CLI scratch
supabase/.temp

# Playwright
e2e/.auth
test-results
playwright-report
.env.test

# ...but the templates must be committed
!.env.local.example
!.env.test.example
